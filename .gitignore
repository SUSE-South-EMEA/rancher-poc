cluster.yml
*.rkestate
kube_config_cluster.yml
kuberang-linux-amd64
proxyCA.pem
.rancher-poc.state
*.log

# Runtime configs (homelab-specific)
configs/test-lab.sh
config.yaml
hosts.list
extra/capi/configs/capi-homelab.sh

# TLS keys and certificates (runtime artifacts)
*.key
*.crt
*.pem
cacerts.pem

# Binaries and archives
kubectl
*.tar.gz

# Kube-vip manifests (generated)
kube-vip.yaml
kube-vip-rbac.yaml
